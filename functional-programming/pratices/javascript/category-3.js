// @004 countNeg
const isNeg = (number) => number < 0
// const countNeg = (arr) => arr.filter(isNeg).length
// const countNeg = (index) => (arr) =>
//   arr.length
//     ? isNeg(arr[index])
//       ? 1 + countNeg(index + 1)(arr.length - 1)
//       : countNeg(index + 1)(arr.length - 1)
//     : 0
const count = (min) => (max) => {
  if (min < max) {
  }
}
function enhancer(arr) {
  const initial = count(0)
}
const arr = [1, -1, 2, -3, 4]

for (let i = 0; i < arr.length; i++) {
  console.log(count(1)(1))
}

// console.log(countNeg(arr))
